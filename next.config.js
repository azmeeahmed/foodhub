/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow cross-origin iframes for Instagram embeds
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Frame-Options',
            value: 'SAMEORIGIN',
          },
        ],
      },
    ];
  },
};

module.exports = nextConfig;
